# ===== Sao chép file này thành .env rồi chỉnh sửa =====
# cp .env.example .env   (Windows: copy .env.example .env)

# Cổng mở ra ngoài để truy cập app (http://localhost:CỔNG)
APP_PORT=3000

# ----- PostgreSQL (chạy trong container) -----
POSTGRES_USER=ktx
POSTGRES_PASSWORD=doi_mat_khau_nay
POSTGRES_DB=ktx

# ----- Bảo mật đăng nhập -----
# Đổi thành 1 chuỗi ngẫu nhiên dài, giữ bí mật
JWT_SECRET=hay_doi_chuoi_bi_mat_that_dai_va_ngau_nhien

# ----- Tài khoản quản trị được tạo sẵn lần đầu -----
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Tên ký túc xá hiển thị
DORM_NAME=Ký túc xá Học viên
